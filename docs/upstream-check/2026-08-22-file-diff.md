# ファイル差分（2026-08-22）

## blob ハッシュ

| ファイル | local HEAD | upstream HEAD | 判定 |
| --- | --- | --- | --- |
| `read_books.py` | `6b0a178` | `3439223`（= 共通祖先と同じ） | local だけ改変。upstream はコード未更新 |
| `requirements.txt` | `e7c0d27` | `e7c0d27` | 同一 |
| `LICENCE` | `38bec6a` | `38bec6a` | 同一 |

## upstream が fork 以降に触ったファイル

- `README.md` のみ

## local にあって upstream にないファイル（book_analysis 除く）

- `.gitignore`
- `docs/superpowers/plans/2026-04-04-batch-performance-optimization.md`
- `run_all_books.py`
- `tests/test_parallel_batch.py`
- `tests/test_skip_heuristic.py`

## upstream にあって local にないファイル

なし（ルートの PDF サンプルは `.gitignore` で追跡対象外。コード追加はない）

## `read_books.py`

upstream の `read_books.py` は 2024-12-28 の初回アップロード以降、コミット履歴上は未変更。
local 側は Z.AI/Anthropic 移行、resume、並列バッチなどを入れているため、upstream を取り込む意味のあるコード差分はない。
