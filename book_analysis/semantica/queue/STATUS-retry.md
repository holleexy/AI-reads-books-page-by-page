# 失敗6冊の再実行

前回は LLM 403（OAuth2 access token could not be validated）で落ちた。グラフは残っていない。`--limit 80`、並列最大 2、`--force`。労務入門は対象外。

進捗: `book_analysis/semantica/queue/run-20260831-135849/`
キュー: `book_analysis/semantica/queue/pending-retry-fails.txt`
